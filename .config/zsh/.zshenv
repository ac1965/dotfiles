#!/bin/zsh
#
# .zshenv (XDG) - sourced by ~/.zshenv once $ZDOTDIR is known.
# Only put additions here, not the bootstrap (XDG_*, ZDOTDIR, path)
# that already lives in ~/.zshenv — this file runs after that one.
#

export AOZORAEPUB3_JAR=$XDG_DATA_HOME/aozoraepub3/AozoraEpub3.jar
