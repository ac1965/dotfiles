#
#

# for ac1965
export SNS_USERNAME=ac1965
export GITHUB_TOKEN="<YOUR GITHUB TOKEN>"

export GITHUB_APIURL="https://api.github.com"
export GITHUB_REPOS="${HOME}/devel/src"

export GPG_KEY_ID="<YOUR GPG KEY-ID>"
export PASSWORD_STORE_DIR="${HOME}/Documents/Secret"
test -d ${PASSWORD_STORE_DIR} || install -d ${PASSWORD_STORE_DIR}

export PATH="${HOME}/.bin:$PATH"

test -d ${GITHUB_REPOS} || install -d ${GITHUB_REPOS}
